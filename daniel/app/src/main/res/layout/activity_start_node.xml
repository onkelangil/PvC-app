<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.graungaard.pvc_app.StartNodeActivity"
    android:background="#ff00102d">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="Du vågner op på et hotel, og kan intet huske fra natten før. Du har tømmermænd, og hører din mobil ringe..."
        android:id="@+id/textView"
        android:layout_marginTop="67dp"
        android:layout_alignParentStart="true"
        android:background="#ff00102d"
        android:textColor="#ffffffff"
        android:layout_centerHorizontal="true"
        android:textSize="25dp"
        android:onClick="buttonOnClick" />

    <Button
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:text="Next"
        android:id="@+id/button"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="44dp" />

    <ImageView
        android:layout_width="500dp"
        android:layout_height="200dp"
        android:id="@+id/imageView"
        android:src="@drawable/hangover_b"
        android:layout_toStartOf="@+id/button"
        android:layout_below="@+id/textView"
        android:layout_alignParentEnd="true" />

</RelativeLayout>
